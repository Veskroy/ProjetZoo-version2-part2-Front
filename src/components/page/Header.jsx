import PropTypes from 'prop-types';

// props => {}
export default function Header() {
    return (
        <header className="header">
        </header>
    )
}

Header.PropTypes = {
}

Header.defaultProps = {
}